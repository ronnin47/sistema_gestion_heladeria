<UserControl x:Class="sistema_gestion_heladeria.Controls.produccionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F4F6F9">

    <UserControl.Resources>
        <!-- Estilo personalizado para botones de acción -->
        <Style x:Key="BtnEmpezarStyle" TargetType="Button">
            <Setter Property="Background" Value="#E05275"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C43D61"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A82E4F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnListoStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#059669"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#047857"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollViewer estilizado -->
        <Style TargetType="ScrollViewer">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       HorizontalAlignment="Right"
                                       AutomationProperties.AutomationId="VerticalScrollBar"
                                       Cursor="Arrow"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Minimum="0"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Width="6"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= COLUMNA 1: PENDIENTES ================= -->
        <Border Grid.Column="0" Background="#EBEFF5" CornerRadius="12" Margin="6" Padding="12">
            <DockPanel LastChildFill="True">
                <!-- Encabezado -->
                <DockPanel DockPanel.Dock="Top" Margin="4,4,4,12">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="10" Height="10" CornerRadius="5" Background="#E05275" Margin="0,0,8,0"/>
                        <TextBlock Text="Pendientes" FontWeight="Bold" FontSize="16" Foreground="#1F2937"/>
                    </StackPanel>

                    <Border Background="#FEE2E2" CornerRadius="12" HorizontalAlignment="Right" Padding="10,3">
                        <TextBlock x:Name="lblCantPendientes" Text="0" FontWeight="Bold" FontSize="13" Foreground="#991B1B"/>
                    </Border>
                </DockPanel>

                <!-- Tarjetas Pendientes -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="2">
                    <ItemsControl x:Name="icPendientes">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" CornerRadius="10" Margin="0,0,0,12" Padding="0,0,0,0" BorderBrush="#E5E7EB" BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05" Color="#000000"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Indicador lateral de color -->
                                        <Border Background="#E05275" CornerRadius="10,0,0,10"/>

                                        <StackPanel Grid.Column="1" Margin="14">
                                            <TextBlock Text="{Binding HeaderText}" Foreground="#6B7280" FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="Bold" FontSize="15" Foreground="#111827" Margin="0,4,0,6"/>

                                            <Border Background="#F9FAFB" CornerRadius="6" Padding="8" Margin="0,2,0,10">
                                                <TextBlock Text="{Binding DetalleFormateado}" Foreground="#374151" FontSize="13" TextWrapping="Wrap"/>
                                            </Border>

                                            <Button Content="Empezar preparación" 
                                                    Style="{StaticResource BtnEmpezarStyle}"
                                                    HorizontalAlignment="Stretch"
                                                    Click="BtnEmpezar_Click" 
                                                    Tag="{Binding PedidoOriginal}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- ================= COLUMNA 2: EN PREPARACIÓN ================= -->
        <Border Grid.Column="1" Background="#EBEFF5" CornerRadius="12" Margin="6" Padding="12">
            <DockPanel LastChildFill="True">
                <!-- Encabezado -->
                <DockPanel DockPanel.Dock="Top" Margin="4,4,4,12">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="10" Height="10" CornerRadius="5" Background="#D97706" Margin="0,0,8,0"/>
                        <TextBlock Text="En preparación" FontWeight="Bold" FontSize="16" Foreground="#1F2937"/>
                    </StackPanel>

                    <Border Background="#FEF3C7" CornerRadius="12" HorizontalAlignment="Right" Padding="10,3">
                        <TextBlock x:Name="lblCantPreparacion" Text="0" FontWeight="Bold" FontSize="13" Foreground="#92400E"/>
                    </Border>
                </DockPanel>

                <!-- Tarjetas En Preparación -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="2">
                    <ItemsControl x:Name="icEnPreparacion">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" CornerRadius="10" Margin="0,0,0,12" Padding="0,0,0,0" BorderBrush="#E5E7EB" BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05" Color="#000000"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Indicador lateral de color -->
                                        <Border Background="#D97706" CornerRadius="10,0,0,10"/>

                                        <StackPanel Grid.Column="1" Margin="14">
                                            <TextBlock Text="{Binding HeaderText}" Foreground="#6B7280" FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="Bold" FontSize="15" Foreground="#111827" Margin="0,4,0,6"/>

                                            <Border Background="#F9FAFB" CornerRadius="6" Padding="8" Margin="0,2,0,10">
                                                <TextBlock Text="{Binding DetalleFormateado}" Foreground="#374151" FontSize="13" TextWrapping="Wrap"/>
                                            </Border>

                                            <Button Content="Marcar listo" 
                                                    Style="{StaticResource BtnListoStyle}"
                                                    HorizontalAlignment="Stretch"
                                                    Click="BtnListo_Click" 
                                                    Tag="{Binding PedidoOriginal}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- ================= COLUMNA 3: LISTOS ================= -->
        <Border Grid.Column="2" Background="#EBEFF5" CornerRadius="12" Margin="6" Padding="12">
            <DockPanel LastChildFill="True">
                <!-- Encabezado -->
                <DockPanel DockPanel.Dock="Top" Margin="4,4,4,12">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="10" Height="10" CornerRadius="5" Background="#10B981" Margin="0,0,8,0"/>
                        <TextBlock Text="Listos" FontWeight="Bold" FontSize="16" Foreground="#1F2937"/>
                    </StackPanel>

                    <Border Background="#D1FAE5" CornerRadius="12" HorizontalAlignment="Right" Padding="10,3">
                        <TextBlock x:Name="lblCantListos" Text="0" FontWeight="Bold" FontSize="13" Foreground="#065F46"/>
                    </Border>
                </DockPanel>

                <!-- Tarjetas Listos -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="2">
                    <ItemsControl x:Name="icListos">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" CornerRadius="10" Margin="0,0,0,12" Padding="0,0,0,0" BorderBrush="#E5E7EB" BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.05" Color="#000000"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="5"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Indicador lateral de color -->
                                        <Border Background="#10B981" CornerRadius="10,0,0,10"/>

                                        <StackPanel Grid.Column="1" Margin="14">
                                            <TextBlock Text="{Binding HeaderText}" Foreground="#6B7280" FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ClienteNombre}" FontWeight="Bold" FontSize="15" Foreground="#111827" Margin="0,4,0,6"/>

                                            <Border Background="#F9FAFB" CornerRadius="6" Padding="8">
                                                <TextBlock Text="{Binding DetalleFormateado}" Foreground="#374151" FontSize="13" TextWrapping="Wrap"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>